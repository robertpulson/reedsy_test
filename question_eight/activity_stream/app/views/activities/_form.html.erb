<%= form_for [@new_activity] do |f| %>
  <%= f.text_field :actor, placeholder: 'Who did it?' %>
  <%= f.select :action, options_for_select(Activity::ACTION_OPTIONS) %>
  <%= f.select :item, options_for_select(Activity::ITEM_OPTIONS) %>
  <br>
  <%= f.submit %>
<% end %>
